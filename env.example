# Sir integration (intent-ai-llama + intent-ai-nextjs-v2)
# Copy to .env and set values. Vite uses VITE_* in the client.

# LLaMA / OpenAI-compatible intent parsing (optional)
VITE_USE_LLAMA=false
VITE_LLAMA_ENDPOINT=https://api.groq.com/openai/v1/chat/completions
VITE_LLAMA_API_KEY=
VITE_LLAMA_MODEL=llama3-70b-8192

# Engines (production uses /api/* via Nginx)
# Local dev: leave unset to use http://localhost:7001, 7002, …; or set /api/intent etc.
# and run engines on 7001–7007 — Vite proxies /api/intent → 7001, /api/decision → 7003, etc.
VITE_INTENT_ENGINE_URL=
VITE_COMPLIANCE_ENGINE_URL=
VITE_DECISION_ENGINE_URL=
VITE_ACTION_ENGINE_URL=
VITE_RISK_ENGINE_URL=
VITE_EXPLAINABILITY_ENGINE_URL=
VITE_EVIDENCE_ENGINE_URL=

# Email service. In dev, app uses /api/email (proxied to localhost:7008). Set VITE_EMAIL_SERVICE_URL to override.
VITE_EMAIL_SERVICE_URL=

# RAG / Ollama (optional, fallback insights used when unset)
VITE_OLLAMA_URL=

# data.gov.in RERA APIs (India). Get keys from data.gov.in → My Account. In dev, app uses /api/rera (proxied to api.data.gov.in) to avoid CORS.
VITE_DATA_GOV_IN_RERA1_KEY=
VITE_DATA_GOV_IN_RERA2_KEY=
# Override RERA base URL only if needed (e.g. prod proxy). Leave unset to use /api/rera in dev and https://api.data.gov.in in prod.
# VITE_RERA_API_BASE=

# Video (LiveKit). Token URL: in dev, /api/video/token is proxied to localhost:3001. Set VITE_LIVEKIT_TOKEN_URL for prod or when token server runs elsewhere.
VITE_LIVEKIT_TOKEN_URL=
VITE_LIVEKIT_URL=
